using UnityEngine;using System.Collections;public class Mover : MonoBehaviour {    void Start ()    {        }        void Update ()    {        float time = Time.timeSinceLevelLoad * 0.4f;        transform.rotation = Quaternion.Euler( 360.0f * Mathf.Sin(time), 360.0f * Mathf.Cos(time), 0.0f);/*        Vector3 v = transform.position;        v.z = 0.5f + Mathf.Sin(time) * 0.15f;        transform.position = v;*/    }}